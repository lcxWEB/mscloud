server:
  port: 3377

spring:
  application:
    name: nacos-config-client
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848 #服务注册中心地址
      config:
        server-addr: localhost:8848 #配置中心地址
        file-extension: yml #指定yaml格式的配置
        group: DEV_GROUP
#        group: TEST_GROUP
        namespace: 75876f34-02bc-424f-8f57-57c0a3ed5e06
        ext-config: